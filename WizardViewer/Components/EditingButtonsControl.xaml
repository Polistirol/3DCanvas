<UserControl x:Class="WizardViewer.Components.EditingButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WizardViewer.Components"
             mc:Ignorable="d" 
              d:DesignWidth="400"  d:Background="White" Height="35" FontSize="11" FontFamily="Comfortaa">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="UnlockBtn" d:Content="Unlock Editing" Content ="{Binding LockingName}" Grid.Column="0" Margin="2" Click="UnlockBtn_Click"/>
        <Button x:Name="PreviewBtn" Content="Preview" Grid.Column="1" Margin="2" IsEnabled="{Binding IsUnlocked}" Click="PreviewBtn_Click"/>
        <Button x:Name="RevertBtn" Content="Revert All" Grid.Column="2" Margin="2" IsEnabled="{Binding IsUnlocked}" Click="RevertBtn_Click" />
        <Button x:Name="SaveBtn" Content="Save" Grid.Column="3" Margin="2" IsEnabled="{Binding IsUnlocked}" Click="SaveBtn_Click"/>
    </Grid>
</UserControl>
